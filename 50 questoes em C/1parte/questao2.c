#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main() {

    int numeros[10];
    int i;
    int length;

    for (i=0; i<10; i++) {
    printf("Digite dez1 números com o ultimo sendo o 0 (%d) \n", i+1);
    scanf("%d", &numeros[i]);

    }
    
}